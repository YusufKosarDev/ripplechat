# Copy to .env. In dev these stay empty/relative — the Vite proxy forwards to
# the backend (see vite.config.ts). For a non-proxied build, set absolute URLs,
# e.g. VITE_API_URL=http://localhost:8081 and VITE_WS_URL=http://localhost:8081/ws
VITE_API_URL=
VITE_WS_URL=/ws
